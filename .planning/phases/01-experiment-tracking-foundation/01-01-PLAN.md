---
phase: 01-experiment-tracking-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mlflow_tracking/mlruns/.gitkeep
  - mlflow_tracking/__init__.py
  - mlflow_tracking/config.py
  - mlflow_tracking/tracker.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "MLflow tracking server is initialized with SQLite backend"
    - "Framework can log experiments with timestamps and status"
    - "Framework captures hyperparameters and metrics"
    - "Framework stores artifacts (models, predictions) with references"
    - "Python SDK provides programmatic logging interface"
  artifacts:
    - path: "mlflow_tracking/tracker.py"
      provides: "ExperimentTracker class with log_params, log_metrics, log_artifact methods"
      min_lines: 80
      exports: ["ExperimentTracker"]
    - path: "mlflow_tracking/config.py"
      provides: "MLflow configuration and initialization"
      contains: "MLFLOW_TRACKING_URI"
    - path: "mlflow_tracking/mlruns/.gitkeep"
      provides: "MLflow storage directory"
  key_links:
    - from: "mlflow_tracking/tracker.py"
      to: "mlflow_tracking.config"
      via: "import config"
      pattern: "from.*config import|import.*config"
    - from: "mlflow_tracking/tracker.py"
      to: "mlflow"
      via: "mlflow.start_run, mlflow.log_params, mlflow.log_metrics"
      pattern: "mlflow\\.(start_run|log_params|log_metrics|log_artifact)"
---

<objective>
Initialize MLflow tracking infrastructure with Python SDK for experiment logging

Purpose: Establish the foundation for reproducible experiment tracking by setting up MLflow with local SQLite backend and creating a Python SDK wrapper for consistent experiment logging. This addresses TRACK-01 (experiment recording with timestamp/status/duration), TRACK-02 (hyperparameter capture), TRACK-03 (metrics recording), TRACK-04 (artifact storage), and TRACK-05 (Python SDK).

Output: Working MLflow tracking server, Python SDK for logging experiments, example usage demonstrating full logging workflow
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Initialize MLflow tracking infrastructure</name>
  <files>mlflow_tracking/mlruns/.gitkeep, mlflow_tracking/__init__.py, mlflow_tracking/config.py</files>
  <action>
    Create MLflow tracking directory structure and configuration:

    1. Create directory structure:
       - mlflow_tracking/
         - __init__.py
         - config.py
         - tracker.py
         - mlruns/.gitkeep

    2. In config.py, define:
       - MLFLOW_TRACKING_URI = "sqlite:///mlflow_tracking/mlruns.db"
       - MLFLOW_ARTIFACT_ROOT = "mlflow_tracking/artifacts"
       - Helper function get_mlflow_config() returning dict with tracking_uri and artifact_root

    3. In __init__.py, export ExperimentTracker class (will be defined in tracker.py)

    Do NOT start MLflow UI server (that's for manual user invocation). Focus on programmatic configuration only.
  </action>
  <verify>
    - Directory mlflow_tracking/ exists with __init__.py, config.py
    - File mlflow_tracking/mlruns/.gitkeep exists
    - config.py defines MLFLOW_TRACKING_URI with sqlite:// path
  </verify>
  <done>
    MLflow directory structure created, SQLite backend configured for local tracking
  </done>
</task>

<task type="auto">
  <name>Implement ExperimentTracker Python SDK</name>
  <files>mlflow_tracking/tracker.py</files>
  <action>
    Create ExperimentTracker class in tracker.py with methods:

    class ExperimentTracker:
        def __init__(self, experiment_name: str, tracking_uri: str = None):
            """Initialize MLflow client with optional tracking URI override"""
            self.client = MLflowClient(tracking_uri)
            self.experiment = self.client.get_experiment_by_name(experiment_name)
            if not self.experiment:
                self.experiment = self.client.create_experiment(experiment_name)
            self.active_run = None

        def start_run(self, run_name: str = None, tags: dict = None) -> str:
            """Start MLflow run, set status=running, return run_id"""
            self.active_run = mlflow.start_run(run_name=run_name, tags=tags)
            mlflow.set_tag("status", "running")
            mlflow.set_tag("start_time", datetime.now().isoformat())
            return self.active_run.info.run_id

        def log_params(self, params: dict):
            """Log hyperparameters/config dict to MLflow"""
            mlflow.log_params(params)

        def log_metrics(self, metrics: dict, step: int = None):
            """Log metrics dict to MLflow (RMSE, R², MAE per target)"""
            mlflow.log_metrics(metrics, step=step)

        def log_artifact(self, file_path: str, artifact_path: str = None):
            """Log artifact (model file, predictions CSV, analysis output)"""
            mlflow.log_artifact(file_path, artifact_path)

        def end_run(self, status: str = "completed"):
            """End run, set final status and duration"""
            if self.active_run:
                duration = time.time() - self.active_run.info.start_time / 1000
                mlflow.set_tag("status", status)
                mlflow.set_tag("duration", duration)
                mlflow.set_tag("end_time", datetime.now().isoformat())
                mlflow.end_run()
                self.active_run = None

        def __enter__(self):
            """Context manager entry"""
            return self

        def __exit__(self, exc_type, exc_val, exc_tb):
            """Context manager exit - mark failed if exception raised"""
            if exc_type is not None:
                self.end_run(status="failed")
            else:
                self.end_run(status="completed")

    Import required modules: mlflow, mlflow.client.MLflowClient, datetime, time
  </action>
  <verify>
    - tracker.py defines ExperimentTracker class
    - Methods: __init__, start_run, log_params, log_metrics, log_artifact, end_run, __enter__, __exit__
    - Class uses mlflow for all tracking operations
  </verify>
  <done>
    ExperimentTracker provides Python SDK for logging experiments with parameters, metrics, artifacts, and automatic status tracking
  </done>
</task>

<task type="auto">
  <name>Create example usage and test tracker</name>
  <files>mlflow_tracking/example_usage.py</files>
  <action>
    Create example_usage.py demonstrating full workflow:

    1. Import ExperimentTracker
    2. Create tracker instance: tracker = ExperimentTracker("test_experiment")
    3. Start run: run_id = tracker.start_run("test_run", tags={"purpose": "demo"})
    4. Log params: tracker.log_params({"learning_rate": 0.01, "max_depth": 5})
    5. Log metrics: tracker.log_metrics({"train_rmse": 10.5, "val_rmse": 12.3, "r2": 0.85})
    6. Create dummy artifact file and log: tracker.log_artifact("dummy_model.pkl")
    7. End run: tracker.end_run(status="completed")

    Also demonstrate context manager usage:
    with ExperimentTracker("context_test") as tracker:
        tracker.start_run("context_run")
        tracker.log_params({"param1": "value1"})
        # Simulate work
        tracker.log_metrics({"metric1": 0.95})

    Test by running: python mlflow_tracking/example_usage.py
    Verify MLflow UI shows the logged experiment (user can run `mlflow ui` to verify)
  </action>
  <verify>
    - example_usage.py runs without errors
    - MLflow SQLite database created at mlflow_tracking/mlruns.db
    - Database contains 2 experiments (test_experiment, context_test) with logged params, metrics, tags
  </verify>
  <done>
    Example demonstrates full tracking workflow, confirms MLflow backend works, provides reference for future usage
  </done>
</task>

</tasks>

<verification>
Overall phase verification:
1. MLflow database contains logged experiments with all required fields (timestamp, status, duration, params, metrics, artifacts)
2. ExperimentTracker can be imported and used in training scripts
3. Context manager properly marks runs as failed/completed based on exceptions
4. All TRACK-01 through TRACK-05 requirements are satisfied
</verification>

<success_criteria>
1. User can instantiate ExperimentTracker and log experiments programmatically
2. MLflow database stores experiments with timestamps, status, duration
3. Hyperparameters and metrics are queryable from MLflow
4. Artifacts (models, predictions) are stored and referenced by runs
5. Example script demonstrates complete logging workflow
</success_criteria>

<output>
After completion, create `.planning/phases/01-experiment-tracking-foundation/01-01-SUMMARY.md`
</output>
