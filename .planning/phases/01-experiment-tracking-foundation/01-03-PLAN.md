---
phase: 01-experiment-tracking-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - mlflow_tracking/tracker.py
  - mlflow_tracking/environment.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Framework automatically tracks Python environment (package versions)"
    - "Framework tracks git commit hash for reproducibility"
    - "Framework tracks random seed for each experiment"
    - "Environment metadata is automatically logged with each run"
  artifacts:
    - path: "mlflow_tracking/environment.py"
      provides: "Environment capture utilities: get_environment(), get_git_hash(), get_package_versions()"
      min_lines: 60
      exports: ["get_environment", "get_git_hash", "get_package_versions"]
    - path: "mlflow_tracking/tracker.py"
      provides: "Updated ExperimentTracker with auto-logging of environment"
      contains: "log_environment method"
  key_links:
    - from: "mlflow_tracking/environment.py"
      to: "mlflow_tracking/tracker.py"
      via: "import get_environment"
      pattern: "from.*environment import get_environment"
    - from: "mlflow_tracking/tracker.py"
      to: "mlflow"
      via: "mlflow.log_params for environment metadata"
      pattern: "mlflow\\.log_dict|mlflow\\.log_params.*environment"
---

<objective>
Implement automatic environment and reproducibility tracking

Purpose: Address REPRO-01 (track Python environment) and REPRO-03 (log ALL experiments including failures) by automatically capturing and logging environment metadata with each experiment run. This ensures every experiment is reproducible by recording package versions, git commit, system info, and random seed.

Output: Environment capture module, updated ExperimentTracker with auto-logging, demonstration of environment tracking in action
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-experiment-tracking-foundation/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Implement environment capture utilities</name>
  <files>mlflow_tracking/environment.py</files>
  <action>
    Create environment.py module with functions to capture reproducibility metadata:

    import subprocess
    import platform
    from typing import Dict, List
    import sys

    def get_git_hash() -> str:
        """Get current git commit hash. Returns 'unknown' if not git repo."""
        try:
            result = subprocess.run(
                ["git", "rev-parse", "HEAD"],
                capture_output=True, text=True, check=True
            )
            return result.stdout.strip()
        except (subprocess.CalledProcessError, FileNotFoundError):
            return "unknown"

    def get_git_branch() -> str:
        """Get current git branch. Returns 'unknown' if not git repo."""
        try:
            result = subprocess.run(
                ["git", "rev-parse", "--abbrev-ref", "HEAD"],
                capture_output=True, text=True, check=True
            )
            return result.stdout.strip()
        except (subprocess.CalledProcessError, FileNotFoundError):
            return "unknown"

    def get_package_versions(packages: List[str] = None) -> Dict[str, str]:
        """
        Get versions of specified packages.
        If packages is None, capture common ML packages.
        """
        if packages is None:
            packages = [
                "python", "numpy", "pandas", "scikit-learn",
                "torch", "xgboost", "mlflow", "shap"
            ]

        versions = {}

        # Python version
        versions["python"] = sys.version.split()[0]

        # Package versions
        for package in packages:
            if package == "python":
                continue
            try:
                if package == "torch":
                    import torch
                    versions[package] = torch.__version__
                elif package == "xgboost":
                    import xgboost
                    versions[package] = xgboost.__version__
                else:
                    module = __import__(package)
                    version = getattr(module, "__version__", "unknown")
                    versions[package] = version
            except ImportError:
                versions[package] = "not_installed"

        return versions

    def get_system_info() -> Dict[str, str]:
        """Get system information"""
        return {
            "os": platform.system(),
            "os_version": platform.version(),
            "architecture": platform.machine(),
            "processor": platform.processor(),
            "python_version": sys.version,
            "python_implementation": platform.python_implementation()
        }

    def get_environment(packages: List[str] = None) -> Dict[str, Dict]:
        """
        Capture complete environment metadata for reproducibility.

        Returns dict with:
        - git: {commit_hash, branch}
        - system: {os, os_version, architecture, ...}
        - packages: {package_name: version}
        """
        return {
            "git": {
                "commit_hash": get_git_hash(),
                "branch": get_git_branch()
            },
            "system": get_system_info(),
            "packages": get_package_versions(packages)
        }

    def log_environment_to_mlflow(env: Dict = None):
        """
        Log environment metadata to active MLflow run.
        Logs as tags for git/system info, params for package versions.
        """
        if env is None:
            env = get_environment()

        import mlflow

        # Log git and system info as tags (not hyperparameters)
        mlflow.set_tags({
            "git.commit_hash": env["git"]["commit_hash"],
            "git.branch": env["git"]["branch"],
            "system.os": env["system"]["os"],
            "system.architecture": env["system"]["architecture"],
            "python.version": env["system"]["python_version"]
        })

        # Log package versions as params
        mlflow.log_params({f"env.{pkg}": ver for pkg, ver in env["packages"].items()})

        return env
  </action>
  <verify>
    - environment.py defines get_environment, get_git_hash, get_package_versions, get_system_info, log_environment_to_mlflow
    - Functions handle missing git and missing packages gracefully
    - get_environment returns nested dict with git/system/packages
  </verify>
  <done>
    Environment capture utilities automatically track git commit, system info, and package versions
  </done>
</task>

<task type="auto">
  <name>Integrate environment tracking into ExperimentTracker</name>
  <files>mlflow_tracking/tracker.py</files>
  <action>
    Update ExperimentTracker class to auto-log environment:

    1. Add import at top:
       from .environment import get_environment, log_environment_to_mlflow

    2. Add auto_log_environment parameter to __init__:
       def __init__(self, experiment_name: str, tracking_uri: str = None, auto_log_environment: bool = True):
           # ... existing code ...
           self.auto_log_environment = auto_log_environment

    3. Update start_run to log environment automatically:
       def start_run(self, run_name: str = None, tags: dict = None, random_seed: int = None) -> str:
           """Start MLflow run, log environment and random seed"""
           self.active_run = mlflow.start_run(run_name=run_name, tags=tags)
           mlflow.set_tag("status", "running")
           mlflow.set_tag("start_time", datetime.now().isoformat())

           # Auto-log environment if enabled
           if self.auto_log_environment:
               log_environment_to_mlflow()

           # Log random seed if provided
           if random_seed is not None:
               mlflow.log_param("random_seed", random_seed)

           return self.active_run.info.run_id

    4. Add log_environment method for manual calls:
       def log_environment(self, env: dict = None):
           """Manually log environment metadata"""
           log_environment_to_mlflow(env)

    Keep all existing functionality (log_params, log_metrics, log_artifact, end_run, context manager).
  </action>
  <verify>
    - tracker.py imports from environment module
    - __init__ has auto_log_environment parameter
    - start_run calls log_environment_to_mlflow if auto_log_environment is True
    - start_run logs random_seed if provided
    - log_environment method exists for manual logging
  </verify>
  <done>
    ExperimentTracker automatically logs environment metadata and random seed with each run
  </done>
</task>

<task type="auto">
  <name>Test environment tracking with example</name>
  <files>mlflow_tracking/test_environment_tracking.py</files>
  <action>
    Create test_environment_tracking.py demonstrating environment tracking:

    from mlflow_tracking import ExperimentTracker
    import numpy as np

    # Test auto-logging (default)
    print("Testing auto-logging environment...")
    with ExperimentTracker("env_test_auto") as tracker:
        run_id = tracker.start_run("auto_env_run", random_seed=42)

        # Log some experiment params and metrics
        tracker.log_params({"learning_rate": 0.01, "epochs": 100})
        tracker.log_metrics({"train_rmse": 10.5, "val_rmse": 12.3})

        print(f"Run ID: {run_id}")
        print("Environment auto-logged (check MLflow UI)")

    # Test manual environment logging
    print("\nTesting manual environment logging...")
    tracker2 = ExperimentTracker("env_test_manual", auto_log_environment=False)
    tracker2.start_run("manual_env_run")

    # Manually log environment
    from mlflow_tracking.environment import log_environment_to_mlflow
    log_environment_to_mlflow()

    tracker2.log_params({"model": " RandomForest", "n_estimators": 50})
    tracker2.log_metrics({"accuracy": 0.95})
    tracker2.end_run()

    print("\n✓ Environment tracking test complete")
    print("Run 'mlflow ui' to verify environment tags and params were logged")

    Run the script and verify:
    - MLflow contains tags: git.commit_hash, git.branch, system.os, python.version
    - MLflow contains params: env.numpy, env.pandas, env.torch, etc.
    - Random seed is logged as param
  </action>
  <verify>
    - test_environment_tracking.py runs without errors
    - MLflow database contains environment tags and params
    - Git commit hash is captured (if git repo)
    - Package versions are logged for installed packages
    - Random seed appears in logged params
  </verify>
  <done>
    Environment tracking demonstrated working, REPRO-01 satisfied (framework tracks Python environment)
  </done>
</task>

</tasks>

<verification>
Overall phase verification:
1. Environment module captures git hash, branch, system info, package versions
2. ExperimentTracker auto-logs environment on start_run (if auto_log_environment=True)
3. Random seed is logged when provided to start_run
4. MLflow UI shows environment tags and package version params
5. REPRO-01 requirement satisfied (Python environment tracking)
6. REPRO-03 requirement satisfied (all experiments logged including failures via context manager)
</verification>

<success_criteria>
1. get_environment() returns dict with git/system/packages keys
2. ExperimentTracker.start_run() logs environment metadata automatically
3. Git commit hash is captured and stored as tag
4. Package versions (numpy, pandas, sklearn, etc.) are logged as params
5. Random seed is logged as param when provided
6. Test script demonstrates both auto and manual environment logging
</success_criteria>

<output>
After completion, create `.planning/phases/01-experiment-tracking-foundation/01-03-SUMMARY.md`
</output>
