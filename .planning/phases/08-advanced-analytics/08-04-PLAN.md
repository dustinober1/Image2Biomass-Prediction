---
phase: 08-advanced-analytics
plan: 04
type: execute
wave: 4
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - mlflow_tracking/cli.py
  - mlflow_tracking/analytics/reporting.py
  - mlflow_tracking/analytics/templates/error_analysis.html
  - mlflow_tracking/analytics/templates/interpretability_report.html
  - mlflow_tracking/analytics/templates/insights_summary.html
  - mlflow_tracking/analytics/__init__.py
  - examples/configs/analytics/README.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can run error analysis via CLI: exp-analyze-errors"
    - "User can generate interpretability reports via CLI: exp-interpret"
    - "User can generate insights via CLI: exp-insights"
    - "User can generate comprehensive HTML/PDF reports combining all analyses"
    - "CLI commands integrate with existing experiment tracking infrastructure"
    - "Documentation explains how to use all analytics features"
  artifacts:
    - path: "mlflow_tracking/cli.py"
      provides: "CLI commands for analytics (exp-analyze-errors, exp-interpret, exp-insights)"
      exports: ["main_analyze_errors", "exp_analyze_errors_command", "main_interpret", "exp_interpret_command", "main_insights", "exp_insights_command"]
    - path: "mlflow_tracking/analytics/reporting.py"
      provides: "ReportGenerator class for HTML/PDF report generation"
      min_lines: 200
      exports: ["ReportGenerator", "generate_html_report", "generate_pdf_report"]
    - path: "mlflow_tracking/analytics/templates/error_analysis.html"
      provides: "Jinja2 template for error analysis reports"
    - path: "mlflow_tracking/analytics/templates/interpretability_report.html"
      provides: "Jinja2 template for interpretability reports"
    - path: "mlflow_tracking/analytics/templates/insights_summary.html"
      provides: "Jinja2 template for insights summary reports"
    - path: "examples/configs/analytics/README.md"
      provides: "Documentation for analytics CLI commands and workflows"
      min_lines: 300
  key_links:
    - from: "exp_analyze_errors_command"
      to: "ErrorAnalyzer"
      via: "Import and use ErrorAnalyzer for error analysis"
      pattern: "from mlflow_tracking.analytics import ErrorAnalyzer"
    - from: "exp_interpret_command"
      to: "ModelInterpretability"
      via: "Import and use ModelInterpretability for SHAP/ELI5 analysis"
      pattern: "from mlflow_tracking.analytics import ModelInterpretability"
    - from: "exp_insights_command"
      to: "InsightsGenerator"
      via: "Import and use InsightsGenerator for automated insights"
      pattern: "from mlflow_tracking.analytics import InsightsGenerator"
    - from: "ReportGenerator.generate_html_report"
      to: "Jinja2 templates"
      via: "Render HTML reports from templates"
      pattern: "jinja2.*Environment.*FileSystemLoader"
    - from: "ReportGenerator.generate_pdf_report"
      to: "WeasyPrint"
      via: "Convert HTML to PDF using WeasyPrint"
      pattern: "from weasyprint import HTML"
---

<objective>
Create CLI commands and report generation for advanced analytics features

Purpose: Provide convenient command-line interfaces and professional report generation for error analysis, model interpretability, and automated insights

Output: CLI commands (exp-analyze-errors, exp-interpret, exp-insights), ReportGenerator class with Jinja2 templates, and comprehensive documentation
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-advanced-analytics/08-RESEARCH.md
@.planning/phases/08-advanced-analytics/08-01-PLAN.md
@.planning/phases/08-advanced-analytics/08-02-PLAN.md
@.planning/phases/08-advanced-analytics/08-03-PLAN.md
@mlflow_tracking/cli.py
@mlflow_tracking/analytics/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLI commands for analytics features</name>
  <files>mlflow_tracking/cli.py</files>
  <action>
    Add analytics CLI commands to cli.py:

    Create exp_analyze_errors_command(args):
    - Parse arguments:
      * run_id: str (required) - MLflow run ID to analyze
      * predictions_path: str (optional, default="predictions.csv") - Path to predictions artifact
      * output_dir: str (optional, default="error_analysis") - Output directory for plots
      * log_artifacts: bool (optional, default=True) - Log analysis as MLflow artifacts
    - Initialize ErrorAnalyzer()
    - Load run: analyzer.load_run(args.run_id, args.predictions_path)
    - Generate plots:
      * residual_fig = analyzer.plot_residuals()
      * distribution_fig = analyzer.plot_error_distribution()
      * pred_vs_actual_fig = analyzer.plot_prediction_vs_actual()
      * failure_modes = analyzer.identify_failure_modes(n_clusters=3)
    - Save plots to output_dir:
      * residual_fig.savefig(f"{output_dir}/residuals.png")
      * distribution_fig.savefig(f"{output_dir}/error_distribution.png")
      * pred_vs_actual_fig.savefig(f"{output_dir}/prediction_vs_actual.png")
    - Get error statistics: stats = analyzer.get_error_statistics()
    - Print summary:
      * Print error statistics (mean, median, max, std, percentiles)
      * Print failure mode summaries (cluster characteristics)
    - If log_artifacts:
      * Create ExperimentTracker run (child of args.run_id)
      * Log all plots as artifacts
      * Log error statistics as metrics
    - Return 0 on success, 1 on error

    Create exp_interpret_command(args):
    - Parse arguments:
      * run_id: str (required) - MLflow run ID to analyze
      * output_dir: str (optional, default="interpretability") - Output directory for plots
      * plot_type: str (optional, default="summary") - Type of SHAP plot (summary, bar, dependence)
      * max_features: int (optional, default=20) - Max features to show
      * compute_permutation: bool (optional, default=False) - Also compute permutation importance
      * log_artifacts: bool (optional, default=True) - Log analysis as MLflow artifacts
    - Initialize ModelInterpretability()
    - Load model: interpreter._load_model_from_artifacts(args.run_id)
    - Load test data (from run artifacts or user-provided path)
    - Compute SHAP values: shap_values, explainer = interpreter.compute_shap(args.run_id, X_test)
    - Generate plots:
      * importance_fig = interpreter.plot_feature_importance(shap_values, X_test, plot_type=args.plot_type, max_features=args.max_features)
      * local_fig = interpreter.plot_local_explanation(shap_values, X_test, sample_idx=0)
    - If compute_permutation:
      * perm_importance = interpreter.compute_permutation_importance(args.run_id, X_test, y_test)
      * perm_fig = interpreter.plot_permutation_importance(perm_importance)
    - Save plots to output_dir
    - Print summary (top features, SHAP values summary)
    - If log_artifacts:
      * Create ExperimentTracker run (child of args.run_id)
      * Log all plots as artifacts
      * Log feature importance as params
    - Return 0 on success, 1 on error

    Create exp_insights_command(args):
    - Parse arguments:
      * run_ids: str (required) - Comma-separated list of MLflow run IDs
      * metric: str (optional, default="val.rmse") - Metric to analyze
      * group_by: str (optional) - Parameter to group by (e.g., "params.learning_rate")
      * output_dir: str (optional, default="insights") - Output directory for reports
      * min_sample_size: int (optional, default=5) - Minimum runs for statistical testing
      * log_artifacts: bool (optional, default=True) - Log insights as MLflow artifacts
    - Parse run_ids: run_ids_list = args.run_ids.split(",")
    - Initialize InsightsGenerator()
    - Generate insights: insights = generator.generate_insights(run_ids_list, metric=args.metric, group_by=args.group_by, min_sample_size=args.min_sample_size)
    - If insights["status"] == "insufficient_data":
      * Print warning and recommendation
      * Return 1
    - Compute hyperparameter correlations: correlations = generator.compare_hyperparameters(run_ids_list, metric=args.metric)
    - Rank experiments: rankings = generator.rank_experiments(run_ids_list)
    - Print summary:
      * Print best run and metrics
      * Print statistical test results (p-values, effect sizes)
      * Print recommendation
      * Print top hyperparameter correlations
      * Print experiment rankings
    - Save results to output_dir:
      * insights.json: Full insights dict
      * correlations.csv: Hyperparameter correlations
      * rankings.csv: Experiment rankings
    - If log_artifacts:
      * Create ExperimentTracker run
      * Log JSON and CSV files as artifacts
      * Log key metrics as params
    - Return 0 on success, 1 on error

    Add argparse subparsers:
    - Add subparser: analyze_errors_parser = subparsers.add_parser("exp-analyze-errors", help="Analyze prediction errors and identify failure modes")
    - Add subparser: interpret_parser = subparsers.add_parser("exp-interpret", help="Generate model interpretability analysis (SHAP/ELI5)")
    - Add subparser: insights_parser = subparsers.add_parser("exp-insights", help="Generate automated insights from experiment comparisons")

    Follow existing CLI patterns (exp-run, exp-run-batch, exp-run-optimize):
    - Use argparse for argument parsing
    - Return exit codes (0=success, 1=failure)
    - Print progress messages to stdout
    - Print errors to stderr
    - Support --verbose flag for detailed output
  </action>
  <verify>
    Verify CLI commands are added:
    ```bash
    python -c "
    from mlflow_tracking.cli import (
        main_analyze_errors,
        exp_analyze_errors_command,
        main_interpret,
        exp_interpret_command,
        main_insights,
        exp_insights_command
    )
    print('All analytics CLI functions exist')
    "
    ```

    Test CLI argument parsing:
    ```bash
    python -m mlflow_tracking.cli exp-analyze-errors --help
    python -m mlflow_tracking.cli exp-interpret --help
    python -m mlflow_tracking.cli exp-insights --help
    ```

    Verify imports work:
    ```bash
    python -c "
    from mlflow_tracking.analytics import ErrorAnalyzer, ModelInterpretability, InsightsGenerator
    print('Analytics imports successful')
    "
    ```
  </verify>
  <done>
    CLI commands added:
    - exp-analyze-errors: Error analysis with residual plots, error distributions, failure modes
    - exp-interpret: Model interpretability with SHAP plots and permutation importance
    - exp-insights: Automated insights with statistical testing and hyperparameter correlations
    - All commands support output_dir for saving results
    - All commands support log_artifacts for MLflow integration
    - All commands follow existing CLI patterns (argparse, exit codes, verbose output)
    - Subparsers added for all three commands
    - Help text available for all commands
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ReportGenerator class with Jinja2 templates</name>
  <files>mlflow_tracking/analytics/reporting.py</files>
  <files>mlflow_tracking/analytics/templates/error_analysis.html</files>
  <files>mlflow_tracking/analytics/templates/interpretability_report.html</files>
  <files>mlflow_tracking/analytics/templates/insights_summary.html</files>
  <action>
    Create ReportGenerator class in reporting.py:

    - __init__(self, template_dir: str = None)
      Initialize Jinja2 environment:
      * If template_dir is None, use default: mlflow_tracking/analytics/templates
      * Create Jinja2 Environment with FileSystemLoader
      * Add custom filters if needed (e.g., datetime formatting)

    - generate_error_analysis_report(
        self,
        run_id: str,
        error_stats: Dict[str, float],
        plot_paths: Dict[str, str],
        failure_modes: pd.DataFrame,
        output_path: str = "error_analysis_report.html"
      ) -> str
      Generate error analysis HTML report:
      * Load error_analysis.html template
      * Prepare context:
        - run_id: str
        - generated_at: str (datetime.now().strftime("%Y-%m-%d %H:%M:%S"))
        - error_stats: dict (mean_abs_error, median_abs_error, max_abs_error, std_residual, percentiles)
        - plot_paths: dict (residuals_plot, error_distribution_plot, prediction_vs_actual_plot)
        - failure_modes: dict (cluster summaries, sample counts)
      * Render template with context
      * Write HTML to output_path
      * Return output_path

    - generate_interpretability_report(
        self,
        run_id: str,
        feature_importance: pd.DataFrame,
        shap_summary_path: str,
        local_explanation_path: str,
        permutation_importance: Optional[pd.DataFrame] = None,
        output_path: str = "interpretability_report.html"
      ) -> str
      Generate interpretability HTML report:
      * Load interpretability_report.html template
      * Prepare context:
        - run_id: str
        - generated_at: str
        - feature_importance: dict (top features, SHAP values)
        - shap_summary_plot: str (image path or base64 encoded)
        - local_explanation_plot: str (image path or base64 encoded)
        - permutation_importance: dict (if computed, top features with importance scores)
      * Render template with context
      * Write HTML to output_path
      * Return output_path

    - generate_insights_report(
        self,
        run_ids: List[str],
        insights: Dict[str, Any],
        correlations: pd.DataFrame,
        rankings: pd.DataFrame,
        output_path: str = "insights_report.html"
      ) -> str
      Generate insights summary HTML report:
      * Load insights_summary.html template
      * Prepare context:
        - run_ids: list
        - generated_at: str
        - insights: dict (best_run, summary_statistics, statistical_tests, recommendation)
        - correlations: dict (top hyperparameter correlations)
        - rankings: dict (experiment rankings with scores)
      * Render template with context
      * Write HTML to output_path
      * Return output_path

    - generate_comprehensive_report(
        self,
        run_ids: List[str],
        error_analysis: Dict,
        interpretability: Dict,
        insights: Dict,
        output_format: str = "html",
        output_path: str = "comprehensive_report"
      ) -> str
      Generate comprehensive report combining all analyses:
      * Run all analyses (error_analysis, interpretability, insights)
      * Compile all results into single context dict
      * Generate HTML report using comprehensive template (or combine individual templates)
      * If output_format is "pdf":
        - Convert HTML to PDF using WeasyPrint
        - Save to {output_path}.pdf
      * If output_format is "html":
        - Save HTML to {output_path}.html
      * Return output path

    - convert_html_to_pdf(self, html_path: str, pdf_path: str) -> str
      Convert HTML report to PDF using WeasyPrint:
      * Read HTML content from html_path
      * Use WeasyPrint HTML(string=html_content).write_pdf(pdf_path)
      * Handle CSS styling for print layout
      * Return pdf_path

    Create Jinja2 templates in mlflow_tracking/analytics/templates/:

    error_analysis.html:
    - HTML5 structure with CSS styling
    - Title: "Error Analysis Report"
    - Sections:
      * Summary (run_id, generated_at, key statistics)
      * Error Statistics (table with mean, median, std, percentiles)
      * Residual Plot (image)
      * Error Distribution (image)
      * Prediction vs Actual (image)
      * Failure Modes (table with cluster summaries)
    - Use Bootstrap CSS for professional styling
    - Responsive design (mobile-friendly)

    interpretability_report.html:
    - HTML5 structure with CSS styling
    - Title: "Model Interpretability Report"
    - Sections:
      * Summary (run_id, generated_at)
      * Global Feature Importance (table with top features)
      * SHAP Summary Plot (image)
      * Local Explanations (waterfall plots for sample predictions)
      * Permutation Importance (if computed, table and plot)
    - Use Bootstrap CSS
    - Color-coded feature importance (red=high, blue=low)

    insights_summary.html:
    - HTML5 structure with CSS styling
    - Title: "Automated Insights Report"
    - Sections:
      * Summary (run_ids, generated_at, best_run)
      * Statistical Tests (table with test results, p-values, effect sizes)
      * Recommendation (highlighted box)
      * Hyperparameter Correlations (table with top correlations)
      * Experiment Rankings (table with ranked experiments)
    - Use Bootstrap CSS
    - Highlight significant results (green for significant, red for not significant)

    Follow Jinja2 best practices:
    - Use {{ variable }} for variable substitution
    - Use {% for item in list %} for loops
    - Use {% if condition %} for conditionals
    - Use filters (e.g., {{ value|round(2) }}) for formatting
    - Include CSS in <style> tags for standalone HTML
  </action>
  <verify>
    Check reporting.py compiles:
    ```bash
    python -m py_compile mlflow_tracking/analytics/reporting.py
    ```

    Verify ReportGenerator has all required methods:
    ```bash
    python -c "
    from mlflow_tracking.analytics.reporting import ReportGenerator
    methods = [
        'generate_error_analysis_report',
        'generate_interpretability_report',
        'generate_insights_report',
        'generate_comprehensive_report',
        'convert_html_to_pdf'
    ]
    generator = ReportGenerator()
    for method in methods:
        assert hasattr(generator, method), f'Missing method: {method}'
    print('All ReportGenerator methods present')
    "
    ```

    Verify templates exist:
    ```bash
    ls -la mlflow_tracking/analytics/templates/error_analysis.html
    ls -la mlflow_tracking/analytics/templates/interpretability_report.html
    ls -la mlflow_tracking/analytics/templates/insights_summary.html
    ```

    Test Jinja2 integration:
    ```bash
    python -c "
    from mlflow_tracking.analytics.reporting import ReportGenerator
    generator = ReportGenerator()
    print('Jinja2 environment initialized successfully')
    print(f'Template dir: {generator.env.loader.searchpath}')
    "
    ```
  </verify>
  <done>
    ReportGenerator class created with:
    - Jinja2 environment initialization with template loading
    - generate_error_analysis_report() for error analysis HTML reports
    - generate_interpretability_report() for SHAP/ELI5 HTML reports
    - generate_insights_report() for automated insights HTML reports
    - generate_comprehensive_report() for combined analysis reports
    - convert_html_to_pdf() for WeasyPrint PDF conversion
    - Three Jinja2 templates (error_analysis.html, interpretability_report.html, insights_summary.html)
    - Bootstrap CSS styling for professional appearance
    - Responsive design for mobile-friendly viewing
    - Image embedding (local paths or base64 encoding)
    - Table formatting for statistics and rankings
    - Highlighted sections for key findings
  </done>
</task>

<task type="auto">
  <name>Task 3: Create documentation and update package exports</name>
  <files>examples/configs/analytics/README.md</files>
  <files>mlflow_tracking/analytics/__init__.py</files>
  <files>mlflow_tracking/__init__.py</files>
  <files>setup.py</files>
  <action>
    Create examples/configs/analytics/README.md with comprehensive documentation:

    Sections to include:
    1. Overview of Advanced Analytics Features
       - Error analysis (residuals, failure modes)
       - Model interpretability (SHAP, ELI5)
       - Automated insights (statistical testing, hyperparameter correlations)
       - Report generation (HTML/PDF)

    2. Installation
       - Install new dependencies: pip install shap eli5 captum statsmodels weasyprint plotly
       - Verify installation: python -c "import shap, eli5, captum"

    3. CLI Usage
       - exp-analyze-errors: Command syntax, arguments, examples
       - exp-interpret: Command syntax, arguments, examples
       - exp-insights: Command syntax, arguments, examples

    4. Python API Usage
       - ErrorAnalyzer example with code
       - ModelInterpretability example with code
       - InsightsGenerator example with code
       - ReportGenerator example with code

    5. Workflow Examples
       - Example 1: Analyze prediction errors for a single run
       - Example 2: Compare feature importance across multiple models
       - Example 3: Generate insights from hyperparameter optimization
       - Example 4: Create comprehensive report for model selection

    6. Interpreting Results
       - How to read residual plots
       - How to interpret SHAP values
       - How to understand statistical test results
       - How to use recommendations

    7. Best Practices
       - Always log analysis as MLflow artifacts
       - Use appropriate sample sizes for statistical testing
       - Validate assumptions (normality) before interpreting tests
       - Combine multiple analyses for comprehensive understanding

    8. Troubleshooting
       - Common errors and solutions
       - What to do if SHAP computation is slow
       - How to handle missing predictions artifacts
       - ELI5 installation issues

    Update mlflow_tracking/analytics/__init__.py:
    - Export ReportGenerator class
    - Export all analytics classes: ErrorAnalyzer, ModelInterpretability, InsightsGenerator, ReportGenerator
    - Export all visualization functions from visualizations.py
    - Add module docstring explaining analytics capabilities

    Update mlflow_tracking/__init__.py:
    - Add analytics module imports
    - Export ReportGenerator
    - Update __all__ list with all analytics exports

    Update setup.py:
    - Add new dependencies:
      * shap>=0.46.0
      * eli5>=0.13.0
      * captum>=0.7.0 (optional, for PyTorch interpretability)
      * statsmodels>=0.14.0
      * weasyprint>=60.0 (optional, for PDF reports)
      * plotly>=5.18.0 (optional, for interactive plots)
    - Add CLI entry points:
      * exp-analyze-errors: mlflow_tracking.cli:main_analyze_errors
      * exp-interpret: mlflow_tracking.cli:main_interpret
      * exp-insights: mlflow_tracking.cli:main_insights
    - Update package description to mention advanced analytics

    Follow documentation patterns from examples/configs/README.md:
    - Use clear section headers
    - Include code examples with syntax highlighting
    - Provide command-line examples
    - Add troubleshooting section
    - Include links to relevant research papers (SHAP paper, etc.)
  </action>
  <verify>
    Verify documentation exists:
    ```bash
    ls -la examples/configs/analytics/README.md
    cat examples/configs/analytics/README.md | head -50
    ```

    Verify package exports:
    ```bash
    python -c "
    from mlflow_tracking import (
        ErrorAnalyzer,
        ModelInterpretability,
        InsightsGenerator,
        ReportGenerator
    )
    print('All analytics exports successful')
    "
    ```

    Verify CLI entry points in setup.py:
    ```bash
    grep -A 10 "console_scripts" setup.py
    ```

    Verify dependencies in setup.py:
    ```bash
    grep -E "(shap|eli5|captum|statsmodels|weasyprint)" setup.py
    ```

    Test CLI commands (dry-run with --help):
    ```bash
    exp-analyze-errors --help
    exp-interpret --help
    exp-insights --help
    ```
  </verify>
  <done>
    Documentation created with:
    - Comprehensive README in examples/configs/analytics/
    - Installation instructions for all new dependencies
    - CLI usage examples for all three commands
    - Python API usage examples for all analytics classes
    - Workflow examples demonstrating real-world use cases
    - Interpretation guides for all analysis types
    - Best practices and troubleshooting sections
    - Package exports updated in __init__.py files
    - setup.py updated with new dependencies
    - CLI entry points added for all analytics commands
    - Links to relevant research papers and documentation
  </done>
</task>

</tasks>

<verification>
Overall verification checks:

1. CLI commands:
   - exp-analyze-errors command works with run_id and optional arguments
   - exp-interpret command works with run_id and optional arguments
   - exp-insights command works with run_ids and optional arguments
   - All commands follow existing CLI patterns
   - Help text available for all commands

2. ReportGenerator:
   - Can generate HTML reports from analysis results
   - Jinja2 templates render correctly
   - Supports PDF conversion via WeasyPrint
   - Comprehensive report combines all analyses
   - Templates use Bootstrap CSS for professional styling

3. Package integration:
   - All analytics classes exported from mlflow_tracking root
   - ReportGenerator exported and accessible
   - setup.py includes all new dependencies
   - CLI entry points registered correctly

4. Documentation:
   - Comprehensive README with examples
   - Installation instructions clear
   - CLI and API usage documented
   - Workflow examples provided
   - Troubleshooting section included

5. End-to-end testing:
   - Can run exp-analyze-errors on a real MLflow run
   - Can run exp-interpret on a real MLflow run
   - Can run exp-insights on multiple MLflow runs
   - Can generate HTML reports from CLI
   - Can generate PDF reports (if WeasyPrint installed)
</verification>

<success_criteria>
Phase 8 Plan 4 is complete when:

1. User can run error analysis via CLI: exp-analyze-errors <run_id>
2. User can generate interpretability reports via CLI: exp-interpret <run_id>
3. User can generate insights via CLI: exp-insights <run_ids>
4. User can generate comprehensive HTML/PDF reports combining all analyses
5. CLI commands integrate with existing experiment tracking infrastructure
6. ReportGenerator can create professional HTML reports with Jinja2 templates
7. Documentation explains how to use all analytics features
8. All dependencies are added to setup.py
9. CLI entry points are registered correctly

Phase 8 (Advanced Analytics) will be complete with all three requirements (ANALYTICS-01, ANALYTICS-02, ANALYTICS-03) satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/08-advanced-analytics/08-04-SUMMARY.md`
</output>
