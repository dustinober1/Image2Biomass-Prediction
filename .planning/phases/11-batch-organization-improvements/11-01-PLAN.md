---
phase: 11-batch-organization-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mlflow_tracking/batch_executor.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "BatchExecutor creates experiment group before running batch experiments"
    - "Group names are timestamp-based and descriptive"
    - "Batch metadata tags are added to groups"
    - "MLflow UI shows organized batch experiments under group names"
  artifacts:
    - path: "mlflow_tracking/batch_executor.py"
      provides: "Batch execution with experiment group creation"
      contains: "ExperimentOrganizer"
    - path: "mlflow_tracking/organizer.py"
      provides: "Experiment group creation (already exists)"
      contains: "create_group"
  key_links:
    - from: "mlflow_tracking/batch_executor.py"
      to: "mlflow_tracking/organizer.py"
      via: "Import ExperimentOrganizer"
      pattern: "from mlflow_tracking.organizer import ExperimentOrganizer"
    - from: "BatchExecutor.execute_batch"
      to: "ExperimentOrganizer.create_group"
      via: "Method call before execution"
      pattern: "organizer.create_group"
    - from: "BatchExecutor._execute_single_experiment"
      to: "ExperimentTracker.start_run"
      via: "Pass experiment_id parameter"
      pattern: "experiment_id="
---

<objective>
Integrate ExperimentOrganizer with BatchExecutor to automatically create experiment groups for batch runs

Purpose: Improve discoverability of batch experiments by organizing related runs into timestamp-based groups with metadata tags
Output: BatchExecutor that creates MLflow experiment groups for each batch execution
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v1-MILESTONE-AUDIT.md

@mlflow_tracking/organizer.py
@mlflow_tracking/batch_executor.py
@.planning/phases/02-organization-discovery/02-01-SUMMARY.md
@.planning/phases/06-parallel-execution-infrastructure**---batch-execution-and-resource-management/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate ExperimentOrganizer into BatchExecutor</name>
  <files>mlflow_tracking/batch_executor.py</files>
  <action>
    Add ExperimentOrganizer integration to BatchExecutor class:

    1. Import ExperimentOrganizer at top of file:
       ```python
       from mlflow_tracking.organizer import ExperimentOrganizer
       ```

    2. In BatchExecutor.__init__, create ExperimentOrganizer instance:
       ```python
       self.organizer = ExperimentOrganizer()
       ```

    3. Add _generate_batch_group_name() helper method:
       - Generate timestamp-based group name: "batch-YYYY-MM-DD-HHMMSS"
       - Use datetime.now().strftime("%Y-%m-%d-%H%M%S")
       - Return formatted string

    4. Modify execute_batch() method to create group before execution:
       - Call _generate_batch_group_name() to get group name
       - Create group with tags: {"batch_size": str(len(configs)), "source": "batch_executor"}
       - Get experiment_id from create_group() return value
       - Pass experiment_id to _execute_single_experiment() calls

    5. Modify _execute_single_experiment() signature:
       - Add optional parameter: experiment_id: Optional[str] = None
       - Pass experiment_id to tracker.start_run() call

    Preserve all existing functionality while adding group creation. Do NOT modify experiment timestamps or other metadata.
  </action>
  <verify>
    python3 -c "
from mlflow_tracking.batch_executor import BatchExecutor
from mlflow_tracking.organizer import ExperimentOrganizer

# Verify ExperimentOrganizer is imported
assert hasattr(BatchExecutor, '__init__'), 'BatchExecutor exists'

# Create instance and check organizer attribute
executor = BatchExecutor()
assert hasattr(executor, 'organizer'), 'organizer attribute exists'
assert isinstance(executor.organizer, ExperimentOrganizer), 'organizer is ExperimentOrganizer instance'

# Check helper method exists
assert hasattr(executor, '_generate_batch_group_name'), '_generate_batch_group_name method exists'

# Verify group name format
group_name = executor._generate_batch_group_name()
assert group_name.startswith('batch-'), f'Group name starts with batch-: {group_name}'
assert len(group_name.split('-')) == 5, f'Group name has 5 parts: {group_name}'

print('✓ All checks passed')
"
  </verify>
  <done>
    ExperimentOrganizer integrated with BatchExecutor:
    - ExperimentOrganizer imported and instantiated in BatchExecutor.__init__
    - _generate_batch_group_name() method creates timestamp-based group names
    - execute_batch() creates group before running experiments
    - Group includes metadata tags (batch_size, source)
    - experiment_id passed to individual experiment runs
  </done>
</task>

<task type="auto">
  <name>Task 2: Test Batch Group Creation End-to-End</name>
  <files>mlflow_tracking/test_batch_executor.py</files>
  <action>
    Add test case for batch group creation to test_batch_executor.py:

    1. Import required modules:
       ```python
       from mlflow_tracking.organizer import ExperimentOrganizer
       import mlflow
       ```

    2. Add test_batch_group_creation() function:
       - Create BatchExecutor instance
       - Load 2 example batch configs (e.g., 01_effnet_b0_bs16.yaml, 02_effnet_b0_bs32.yaml)
       - Execute batch with max_workers=1 (sequential for testing)
       - Verify group created by checking ExperimentOrganizer.list_groups()
       - Verify group name starts with "batch-"
       - Verify group tags contain "batch_size" and "source"
       - Verify experiment IDs match the created group

    3. Add if __name__ == "__main__" block to run test when executed directly

    Do NOT modify existing test cases. Add new test function at end of file.
  </action>
  <verify>
    python3 mlflow_tracking/test_batch_executor.py
    # Should show existing tests pass plus new test_batch_group_creation test
  </verify>
  <done>
    Test suite validates batch group creation:
    - test_batch_group_creation() function added to test_batch_executor.py
    - Test verifies group name format (batch-YYYY-MM-DD-HHMMSS)
    - Test verifies group metadata tags (batch_size, source)
    - Test verifies experiments created under correct group
    - All existing tests continue to pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Documentation and Examples</name>
  <files>
    mlflow_tracking/__init__.py
    examples/configs/README.md
  </files>
  <action>
    Update documentation to reflect batch group creation:

    1. Update mlflow_tracking/__init__.py if needed (check if ExperimentOrganizer already exported - it should be from Phase 2)

    2. Update examples/configs/README.md Batch Execution section:
       - Add note about automatic group creation
       - Explain group naming convention (batch-YYYY-MM-DD-HHMMSS)
       - Explain group metadata tags (batch_size, source)
       - Add example showing MLflow UI navigation to batch groups

    3. Add inline docstring updates to BatchExecutor.execute_batch():
       - Document automatic group creation behavior
       - Document group naming format
       - Document group metadata tags

    Preserve all existing documentation. Add new sections without modifying existing text.
  </action>
  <verify>
    grep -A 5 "automatic group creation" examples/configs/README.md && \
    grep -A 3 "batch-YYYY-MM-DD" examples/configs/README.md && \
    python3 -c "from mlflow_tracking import BatchExecutor; help(BatchExecutor.execute_batch)" | grep -q "group"
  </verify>
  <done>
    Documentation updated with batch group creation information:
    - README.md explains automatic group creation behavior
    - Group naming convention documented (batch-YYYY-MM-DD-HHMMSS)
    - Group metadata tags documented (batch_size, source)
    - BatchExecutor.execute_batch() docstring updated
    - Users understand how to find batch experiments in MLflow UI
  </done>
</task>

</tasks>

<verification>
Overall phase verification:

1. **Code Quality**: BatchExecutor imports and uses ExperimentOrganizer correctly
2. **Functionality**: Batch groups created with timestamp-based names and metadata tags
3. **Test Coverage**: test_batch_executor.py includes test_batch_group_creation() test case
4. **Documentation**: README.md and docstrings explain group creation behavior
5. **Integration**: MLflow UI shows batch experiments organized under groups

Manual verification (optional but recommended):
- Run exp-run-batch on example configs
- Open MLflow UI (mlflow ui)
- Verify batch group appears in left sidebar
- Click group and verify experiments are listed
- Verify group tags show batch_size and source
</verification>

<success_criteria>
Phase 11 complete when:

1. ✓ BatchExecutor creates experiment groups before running batch experiments
2. ✓ Group names follow format: batch-YYYY-MM-DD-HHMMSS
3. ✓ Group tags include batch_size and source metadata
4. ✓ MLflow UI shows organized batch experiments under groups
5. ✓ Test suite validates group creation behavior
6. ✓ Documentation explains automatic group creation

**Gap 3 from v1-MILESTONE-AUDIT.md is CLOSED.**
</success_criteria>

<output>
After completion, create `.planning/phases/11-batch-organization-improvements/11-01-SUMMARY.md`
</output>
