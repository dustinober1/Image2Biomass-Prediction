---
phase: 10-canonical-splits-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mlflow_tracking/adapters.py
  - mlflow_tracking/config_parser.py
  - scripts/train_oof_effnet.py
  - scripts/train_ridge_advanced.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Adapters load canonical splits from DataSplitter"
    - "Adapters pass split indices to training scripts via CLI args"
    - "Training scripts accept --train-indices, --val-indices, --test-indices flags"
    - "All experiments use identical train/validation/test splits"
  artifacts:
    - path: "mlflow_tracking/adapters.py"
      provides: "PyTorchAdapter and SklearnAdapter with split loading"
      exports: ["PyTorchAdapter", "SklearnAdapter"]
      modifies: "execute() and _execute_with_autolog() methods"
    - path: "mlflow_tracking/config_parser.py"
      provides: "ExperimentConfig schema"
      contains: "ExperimentConfig class with optional fields"
    - path: "scripts/train_oof_effnet.py"
      provides: "PyTorch training with canonical splits"
      modifies: "train_oof() function, adds argparse"
    - path: "scripts/train_ridge_advanced.py"
      provides: "Sklearn training with canonical splits"
      modifies: "train_ridge_advanced() function, adds argparse"
    - path: "data/canonical_splits.json"
      provides: "Canonical split indices"
      exists: true
  key_links:
    - from: "mlflow_tracking/adapters.py"
      to: "mlflow_tracking/data_split.py"
      via: "from mlflow_tracking.data_split import DataSplitter"
      pattern: "DataSplitter.*load_splits"
    - from: "mlflow_tracking/adapters.py"
      to: "scripts/train_oof_effnet.py"
      via: "subprocess args --train-indices, --val-indices, --test-indices"
      pattern: "--train-indices.*--val-indices.*--test-indices"
    - from: "mlflow_tracking/adapters.py"
      to: "scripts/train_ridge_advanced.py"
      via: "subprocess args --train-indices, --val-indices, --test-indices"
      pattern: "--train-indices.*--val-indices.*--test-indices"
    - from: "scripts/train_oof_effnet.py"
      to: "df.iloc[train_idx]"
      via: "argparse parsed indices used for data slicing"
      pattern: "df\.iloc\[.*_idx\]"
    - from: "scripts/train_ridge_advanced.py"
      to: "stacked_df.iloc[train_idx]"
      via: "argparse parsed indices used for data slicing"
      pattern: "stacked_df\.iloc\[.*_idx\]"
---

<objective>
Integrate DataSplitter with adapters to enforce canonical splits across all experiments.

Purpose: Ensure reproducibility by having all experiments use identical train/validation/test splits from canonical_splits.json, preventing data leakage and enabling fair model comparison.

Output: Adapters load and pass split indices to training scripts; training scripts accept and use provided splits via CLI args.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-canonical-splits-enforcement/10-RESEARCH.md
@.planning/phases/01-experiment-tracking-foundation/01-02-SUMMARY.md

# Existing Code
@mlflow_tracking/data_split.py
@mlflow_tracking/adapters.py
@mlflow_tracking/config_parser.py
@data/canonical_splits.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add split loading to PyTorchAdapter</name>
  <files>mlflow_tracking/adapters.py</files>
  <action>
Modify PyTorchAdapter._execute_with_autolog() to load canonical splits and pass as CLI args:

1. Import DataSplitter at top of file (if not already imported):
   from mlflow_tracking.data_split import DataSplitter

2. In _execute_with_autolog() method, BEFORE building args:
   # Load canonical splits
   splitter = DataSplitter(split_file="data/canonical_splits.json")
   splits = splitter.load_splits()

3. When building args, add split indices AFTER script path:
   # Add split indices as comma-separated strings
   train_str = ",".join(map(str, splits["train_indices"]))
   val_str = ",".join(map(str, splits["val_indices"]))
   test_str = ",".join(map(str, splits["test_indices"]))

   args.extend([
       "--train-indices", train_str,
       "--val-indices", val_str,
       "--test-indices", test_str
   ])

4. Keep existing parameter loop after split indices (no changes to param_mapping)

DO NOT modify PyTorchAdapter.validate_config() - splits are always loaded, not part of config
DO NOT modify execute() method signature - only _execute_with_autolog() internals

Reference: Research section "Pattern 1: Adapter Loads and Passes Splits"
  </action>
  <verify>
# Verify imports work
python3 -c "from mlflow_tracking.adapters import PyTorchAdapter; from mlflow_tracking.data_split import DataSplitter; s = DataSplitter().load_splits(); print(f'Loaded {len(s[\"train_indices\"])} train, {len(s[\"val_indices\"])} val, {len(s[\"test_indices\"])} test indices')"

# Verify method contains split loading
grep -n "DataSplitter\|load_splits\|--train-indices" mlflow_tracking/adapters.py
  </verify>
  <done>
PyTorchAdapter._execute_with_autolog() loads canonical splits and passes --train-indices, --val-indices, --test-indices to training script
  </done>
</task>

<task type="auto">
  <name>Task 2: Add split loading to SklearnAdapter</name>
  <files>mlflow_tracking/adapters.py</files>
  <action>
Modify SklearnAdapter._execute_with_autolog() to load canonical splits and pass as CLI args:

1. Import DataSplitter at top of file (should already be imported from Task 1)

2. In SklearnAdapter._execute_with_autolog() method, BEFORE building args:
   # Load canonical splits
   from mlflow_tracking.data_split import DataSplitter
   splitter = DataSplitter(split_file="data/canonical_splits.json")
   splits = splitter.load_splits()

3. When building args (inside the for loop that adds parameters), ADD split indices FIRST:
   args = ["python3", script_path]

   # Add split indices as comma-separated strings
   train_str = ",".join(map(str, splits["train_indices"]))
   val_str = ",".join(map(str, splits["val_indices"]))
   test_str = ",".join(map(str, splits["test_indices"]))

   args.extend([
       "--train-indices", train_str,
       "--val-indices", val_str,
       "--test-indices", test_str
   ])

   # Then add existing parameters
   for param_name, value in config.parameters.items():
       arg_name = f"--{param_name}"
       args.append(arg_name)
       args.append(str(value))

Note: SklearnAdapter uses a simpler parameter loop than PyTorchAdapter. Insert split indices before the loop.

DO NOT modify SklearnAdapter.validate_config() - splits are always loaded, not part of config

Reference: Research section "Pattern 3: SklearnAdapter Follows Same Pattern"
  </action>
  <verify>
# Verify SklearnAdapter has split loading
grep -A 20 "class SklearnAdapter" mlflow_tracking/adapters.py | grep -n "DataSplitter\|load_splits\|--train-indices"

# Verify both adapters load splits
python3 -c "from mlflow_tracking.adapters import PyTorchAdapter, SklearnAdapter; print('Both adapters imported successfully')"
  </verify>
  <done>
SklearnAdapter._execute_with_autolog() loads canonical splits and passes --train-indices, --val-indices, --test-indices to training script
  </done>
</task>

<task type="auto">
  <name>Task 3: Modify train_oof_effnet.py to accept split indices</name>
  <files>scripts/train_oof_effnet.py</files>
  <action>
Modify scripts/train_oof_effnet.py to accept and use canonical splits via CLI args:

1. Add argparse import at top (after existing imports):
   import argparse

2. Add parse_indices() helper function after imports:
   def parse_indices(indices_str):
       \"\"\"Parse comma-separated indices string to list of integers.\"\"\"
       return [int(x.strip()) for x in indices_str.split(",") if x.strip()]

3. Modify train_oof() signature to accept split parameters:
   def train_oof(train_indices=None, val_indices=None, test_indices=None):

4. At the START of train_oof(), BEFORE KFold initialization, add:
   # Use provided split indices if available, otherwise fall back to KFold
   if train_indices is not None and val_indices is not None and test_indices is not None:
       train_idx = parse_indices(train_indices) if isinstance(train_indices, str) else train_indices
       val_idx = parse_indices(val_indices) if isinstance(val_indices, str) else val_indices
       test_idx = parse_indices(test_indices) if isinstance(test_indices, str) else test_indices
       use_canonical_splits = True
   else:
       use_canonical_splits = False

5. Replace the KFold loop with conditional logic:
   if use_canonical_splits:
       # Use canonical splits (single train/val split, not KFold)
       print("Using canonical splits from DataSplitter")
       train_df = df.iloc[train_idx]
       val_df = df.iloc[val_idx]
       test_df = df.iloc[test_idx]

       # Create datasets and loaders
       train_dataset = BiomassDataset(train_df, transform=train_transform)
       val_dataset = BiomassDataset(val_df, transform=val_transform)

       train_loader = DataLoader(train_dataset, batch_size=BATCH_SIZE, shuffle=True)
       val_loader = DataLoader(val_dataset, batch_size=BATCH_SIZE, shuffle=False)

       # Train single model on canonical splits
       model = timm.create_model('efficientnet_b0', pretrained=True, num_classes=5).to(DEVICE)
       # ... existing training loop code ...

       # Note: oof_preds not needed with canonical splits - train once
       # Set oof_preds[val_idx] for compatibility with existing code
       oof_preds = np.zeros((len(df), len(TARGETS)))
       # Evaluate and store predictions
   else:
       # Original KFold code (keep as fallback)
       kf = KFold(n_splits=5, shuffle=True, random_state=42)
       oof_preds = np.zeros((len(df), len(TARGETS)))
       for fold, (train_idx, val_idx) in enumerate(kf.split(df)):
           # ... existing KFold loop code ...

6. Add main() function with argparse at BOTTOM of file (before if __name__ == "__main__"):
   def main():
       parser = argparse.ArgumentParser(description="Train EfficientNet with canonical splits")
       parser.add_argument("--train-indices", type=str, help="Comma-separated train indices")
       parser.add_argument("--val-indices", type=str, help="Comma-separated val indices")
       parser.add_argument("--test-indices", type=str, help="Comma-separated test indices")
       # Note: Keep existing params for backward compatibility
       parser.add_argument("--model_name", type=str, default="efficientnet_b0")
       parser.add_argument("--batch_size", type=int, default=16)
       parser.add_argument("--epochs", type=int, default=30)
       parser.add_argument("--learning_rate", type=float, default=1e-4)

       args = parser.parse_args()

       # Call train_oof with split indices if provided
       if args.train_indices and args.val_indices and args.test_indices:
           train_oof(
               train_indices=args.train_indices,
               val_indices=args.val_indices,
               test_indices=args.test_indices
           )
       else:
           # Fallback to KFold if no split indices provided
           train_oof()

7. Update if __name__ == "__main__" block:
   if __name__ == "__main__":
       main()

IMPORTANT: Keep the KFold code path as fallback for backward compatibility when script is run directly without split indices

Reference: Research section "Pattern 2: Training Scripts Accept Split Indices" and "Training Script Uses Provided Splits"
  </action>
  <verify>
# Verify script accepts split indices
python3 scripts/train_oof_effnet.py --help | grep "train-indices\|val-indices\|test-indices"

# Verify indices are parsed correctly
python3 -c "s='1,2,3'; print([int(x.strip()) for x in s.split(',')])"

# Verify script imports without errors
python3 -c "import train_oof_effnet; print('Import successful')"
  </verify>
  <done>
train_oof_effnet.py accepts --train-indices, --val-indices, --test-indices CLI args and uses them instead of KFold when provided
  </done>
</task>

<task type="auto">
  <name>Task 4: Modify train_ridge_advanced.py to accept split indices</name>
  <files>scripts/train_ridge_advanced.py</files>
  <action>
Modify scripts/train_ridge_advanced.py to accept and use canonical splits via CLI args:

1. Add argparse import at top (after existing imports):
   import argparse

2. Add parse_indices() helper function after imports:
   def parse_indices(indices_str):
       \"\"\"Parse comma-separated indices string to list of integers.\"\"\"
       return [int(x.strip()) for x in indices_str.split(",") if x.strip()]

3. Modify train_ridge_advanced() signature to accept split parameters:
   def train_ridge_advanced(train_indices=None, val_indices=None, test_indices=None):

4. At the START of train_ridge_advanced(), BEFORE KFold initialization, add:
   # Use provided split indices if available, otherwise fall back to KFold
   if train_indices is not None and val_indices is not None and test_indices is not None:
       train_idx = parse_indices(train_indices) if isinstance(train_indices, str) else train_indices
       val_idx = parse_indices(val_indices) if isinstance(val_indices, str) else val_indices
       test_idx = parse_indices(test_indices) if isinstance(test_indices, str) else test_indices
       use_canonical_splits = True
   else:
       use_canonical_splits = False

5. Replace the KFold loop with conditional logic:
   if use_canonical_splits:
       # Use canonical splits (single train/val split, not KFold)
       print("Using canonical splits from DataSplitter")
       train_df = stacked_df.iloc[train_idx]
       val_df = stacked_df.iloc[val_idx]
       test_df = stacked_df.iloc[test_idx]

       # Train model on canonical splits
       for target in TARGETS:
           y = stacked_df[target].values
           X = stacked_df[feature_cols].values

           X_train, X_val = X[train_idx], X[val_idx]
           y_train, y_val = y[train_idx], y[val_idx]

           model = Ridge(alpha=1.0)
           model.fit(X_train, y_train)

           preds = model.predict(X_val)
           # ... existing evaluation code ...

       # Save predictions for error analysis
       predictions_df = pd.DataFrame({
           'image_id': image_ids,
           'actual': all_actuals['Dry_Total_g'],
           'predicted': all_predictions['Dry_Total_g']
       })
       predictions_df.to_csv('predictions.csv', index=False)
   else:
       # Original KFold code (keep as fallback)
       kf = KFold(n_splits=5, shuffle=True, random_state=42)
       overall_rmse = []
       # ... existing KFold loop code ...

6. Add main() function with argparse at BOTTOM of file (before if __name__ == "__main__"):
   def main():
       parser = argparse.ArgumentParser(description="Train Ridge with canonical splits")
       parser.add_argument("--train-indices", type=str, help="Comma-separated train indices")
       parser.add_argument("--val-indices", type=str, help="Comma-separated val indices")
       parser.add_argument("--test-indices", type=str, help="Comma-separated test indices")
       # Note: Keep existing params for backward compatibility
       parser.add_argument("--model_type", type=str, default="ridge")
       parser.add_argument("--alpha", type=float, default=1.0)
       parser.add_argument("--random_seed", type=int, default=42)

       args = parser.parse_args()

       # Call train_ridge_advanced with split indices if provided
       if args.train_indices and args.val_indices and args.test_indices:
           train_ridge_advanced(
               train_indices=args.train_indices,
               val_indices=args.val_indices,
               test_indices=args.test_indices
           )
       else:
           # Fallback to KFold if no split indices provided
           train_ridge_advanced()

7. Update if __name__ == "__main__" block:
   if __name__ == "__main__":
       main()

IMPORTANT: Keep the KFold code path as fallback for backward compatibility when script is run directly without split indices

Reference: Research section "Pattern 2: Training Scripts Accept Split Indices" and sklearn example in "Training Script Uses Provided Splits"
  </action>
  <verify>
# Verify script accepts split indices
python3 scripts/train_ridge_advanced.py --help | grep "train-indices\|val-indices\|test-indices"

# Verify script imports without errors
python3 -c "import train_ridge_advanced; print('Import successful')"

# Verify canonical splits are used when indices provided
python3 -c "
import argparse
import sys
sys.argv = ['train_ridge_advanced.py', '--train-indices', '0,1,2', '--val-indices', '3,4', '--test-indices', '5,6']
# Would trigger argparse - just verify syntax
print('Argparse syntax validated')
"
  </verify>
  <done>
train_ridge_advanced.py accepts --train-indices, --val-indices, --test-indices CLI args and uses them instead of KFold when provided
  </done>
</task>

</tasks>

<verification>
Overall phase verification:

1. Verify adapters load splits:
   python3 -c "
   from mlflow_tracking.adapters import PyTorchAdapter, SklearnAdapter
   from mlflow_tracking.data_split import DataSplitter
   s = DataSplitter().load_splits()
   assert len(s['train_indices']) == 249
   assert len(s['val_indices']) == 54
   assert len(s['test_indices']) == 54
   print('Canonical splits loaded correctly')
   "

2. Verify training scripts accept split args:
   python3 scripts/train_oof_effnet.py --help | grep "train-indices"
   python3 scripts/train_ridge_advanced.py --help | grep "train-indices"

3. Verify end-to-end flow (dry run):
   # This will fail at actual training but verify split passing works
   exp-run examples/configs/adapter_examples/pytorch_effnet.yaml --dry-run

4. Verify no KFold usage when splits provided:
   grep -n "KFold" scripts/train_oof_effnet.py scripts/train_ridge_advanced.py
   # Should only be in fallback path, not primary execution path
</verification>

<success_criteria>
Phase 10 is complete when:
1. PyTorchAdapter loads canonical splits from DataSplitter and passes to train_oof_effnet.py
2. SklearnAdapter loads canonical splits from DataSplitter and passes to train_ridge_advanced.py
3. train_oof_effnet.py accepts --train-indices, --val-indices, --test-indices args
4. train_ridge_advanced.py accepts --train-indices, --val-indices, --test-indices args
5. Both scripts use provided splits instead of KFold when indices are provided
6. KFold fallback remains for direct script execution without split args
7. All experiments now use identical splits from canonical_splits.json (Gap 2 closed)
</success_criteria>

<output>
After completion, create `.planning/phases/10-canonical-splits-enforcement/10-01-SUMMARY.md`
</output>
