---
phase: 07-hyperparameter-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mlflow_tracking/optuna_optimizer.py
  - mlflow_tracking/__init__.py
  - mlflow_tracking/config_parser.py
  - mlflow_tracking/cli.py
  - setup.py
  - examples/configs/optimization/
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can define hyperparameter search spaces in YAML config"
    - "User can run optimization studies that automatically search for best hyperparameters"
    - "Framework prunes underperforming trials early to save computation"
    - "Multiple trials can run in parallel using BatchExecutor"
    - "Optimization results are logged to MLflow for analysis"
    - "Best hyperparameters can be retrieved and applied to new experiments"
  artifacts:
    - path: "mlflow_tracking/optuna_optimizer.py"
      provides: "OptunaOptimizer class for hyperparameter optimization"
      min_lines: 300
      exports: ["OptunaOptimizer", "OptunaConfig"]
    - path: "mlflow_tracking/config_parser.py"
      provides: "OptimizationConfig schema extension"
      contains: "OptimizationConfig"
    - path: "mlflow_tracking/cli.py"
      provides: "CLI extension (exp-run-optimize)"
      exports: ["main_optimize", "exp_run_optimize_command"]
  key_links:
    - from: "mlflow_tracking/optuna_optimizer.py"
      to: "mlflow_tracking/adapters.py"
      via: "AdapterRegistry.get_adapter()"
      pattern: "AdapterRegistry\\.get_adapter"
    - from: "mlflow_tracking/optuna_optimizer.py"
      to: "mlflow_tracking/tracker.py"
      via: "ExperimentTracker for logging trials"
      pattern: "ExperimentTracker"
    - from: "mlflow_tracking/optuna_optimizer.py"
      to: "mlflow_tracking/batch_executor.py"
      via: "BatchExecutor for parallel trials"
      pattern: "BatchExecutor"
    - from: "mlflow_tracking/optuna_optimizer.py"
      to: "optuna"
      via: "optuna.study.create_study()"
      pattern: "optuna\\.study"
---

<objective>
Integrate Optuna for automated hyperparameter optimization with efficient pruning and parallel trial execution

Purpose: Enable systematic hyperparameter search without manual trial-and-error. Optuna will automatically explore hyperparameter spaces, prune underperforming trials early, and log all results to MLflow for analysis. Users define search spaces in YAML config, and the framework handles the rest.

Output: OptunaOptimizer class, OptimizationConfig schema extension, CLI extension (exp-run-optimize), example optimization configs
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OptimizationConfig schema and OptunaOptimizer class</name>
  <files>mlflow_tracking/config_parser.py, mlflow_tracking/optuna_optimizer.py</files>
  <action>
    **A. Extend config_parser.py with OptimizationConfig schema (50 lines):**

    1. Create OptimizationConfig class inheriting from BaseModel with:
       - n_trials: int (Field(default=100, gt=0, description="Number of trials to run"))
       - study_name: str (Field(min_length=1, description="Optuna study name"))
       - direction: Literal["minimize", "maximize"] (Field(default="minimize"))
       - metric: str (Field(min_length=1, description="Metric to optimize (e.g., 'val.rmse')"))
       - search: Dict[str, Dict[str, Any]] (Field(description="Search space definition"))
       - pruner: Optional[Dict[str, Any]] (Field(default=None, description="Pruner config"))
       - timeout: Optional[int] (Field(default=None, description="Study timeout in seconds"))

    2. Define SearchParamConfig schema for search space parameters:
       - type: Literal["float", "int", "categorical"]
       - low: Optional[float] (for float/int ranges)
       - high: Optional[float] (for float/int ranges)
       - log: Optional[bool] (for log-scale sampling)
       - step: Optional[int] (for int discretization)
       - choices: Optional[List[Any]] (for categorical)

    3. Update ExperimentConfig to include optional optimization field:
       - optimization: Optional[OptimizationConfig] (Field(default=None))

    4. Add validation: If optimization present, validate search space types and required fields

    5. Export OptimizationConfig, SearchParamConfig from mlflow_tracking

    **B. Create mlflow_tracking/optuna_optimizer.py with OptunaOptimizer class (350+ lines):**

    1. Import dependencies:
       - optuna
       - optuna.integration.MLflowCallback
       - from mlflow_tracking import ExperimentTracker, AdapterRegistry, ExperimentConfig
       - from typing import Dict, Any, Optional, Callable

    2. Create OptunaOptimizer class with:
       - __init__(self, optimization_config: OptimizationConfig, base_config: ExperimentConfig, tracker: ExperimentTracker)
       - _parse_search_space(self) -> Dict[str, Any]
       - _create_pruner(self) -> optuna.pruners.BasePruner
       - objective(self, trial: optuna.Trial) -> float
       - run_study(self, n_jobs: int = 1, show_progress: bool = True) -> optuna.Study
       - get_best_params(self) -> Dict[str, Any]
       - get_best_trial(self) -> optuna.Trial
       - generate_best_config(self) -> ExperimentConfig

    3. Add helper functions:
       - create_optimization_config(config_dict: Dict[str, Any]) -> OptimizationConfig
       - suggest_params_from_trial(trial: optuna.Trial, search_space: Dict[str, Any]) -> Dict[str, Any]

    4. Export OptunaOptimizer, OptimizationConfig, create_optimization_config from mlflow_tracking

    **C. Update mlflow_tracking/__init__.py (5 lines):**
    - Add imports: from .optuna_optimizer import OptunaOptimizer, OptimizationConfig
    - Add to __all__: ["OptunaOptimizer", "OptimizationConfig"]

    **Key Implementation Details:**
    - Use optuna.integration.MLflowCallback(mlflow_tracking_uri=tracker.tracking_uri) to log trials
    - Store optimization study results in MLflow as params (best_params, best_value)
    - Handle trial failures gracefully (catch exceptions, return inf for minimization)
    - Validate search space before study creation to fail fast on invalid configs
  </action>
  <verify>
    **Verification steps:**
    1. Check mlflow_tracking/optuna_optimizer.py exists (300+ lines)
    2. Check OptimizationConfig class exists in config_parser.py
    3. Check OptunaOptimizer class has all required methods
    4. Check exports in __init__.py include OptunaOptimizer and OptimizationConfig
    5. Run: python -c "from mlflow_tracking import OptunaOptimizer, OptimizationConfig; print('Import successful')"
    6. Run: python -c "from mlflow_tracking.config_parser import ExperimentConfig; print(ExperimentConfig.model_json_schema())" and verify optimization field in schema

    **Expected output:**
    - Import succeeds without errors
    - Schema includes "optimization" field with proper nested structure
  </verify>
  <done>
    OptimizationConfig schema defines search space for hyperparameters
    OptunaOptimizer class creates and runs Optuna studies with MLflow logging
    Search space parsing supports float, int, and categorical parameter types
    Pruner creation supports median, hyperband, and successive halving algorithms
    Objective function executes trials using adapters and returns metric values
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CLI extension for optimization (exp-run-optimize)</name>
  <files>mlflow_tracking/cli.py, setup.py</files>
  <action>
    **A. Extend mlflow_tracking/cli.py with optimization commands (150+ lines):**

    1. Import dependencies:
       - from mlflow_tracking import OptunaOptimizer, OptimizationConfig, ConfigParser, ExperimentTracker
       - from pathlib import Path

    2. Create exp_run_optimize_command function with proper error handling and exit codes

    3. Create main_optimize function as CLI entry point with argparse

    4. Add docstrings and type hints for all functions

    **B. Update setup.py (5 lines):**
    - Add exp-run-optimize to console_scripts entry point

    **C. Update mlflow_tracking/__init__.py (2 lines):**
    - Add imports and exports for main_optimize, exp_run_optimize_command

    **Key Implementation Details:**
    - Use ConfigParser.load_config() to load YAML configs
    - Use ExperimentTracker for MLflow logging
    - Save best config as YAML for easy reuse
    - Support n_jobs=-1 for auto-detect parallel trials
    - Print progress with show_progress_bar=True from Optuna
  </action>
  <verify>
    **Verification steps:**
    1. Check cli.py has exp_run_optimize_command and main_optimize functions
    2. Check setup.py has exp-run-optimize entry point
    3. Check __init__.py exports main_optimize and exp_run_optimize_command
    4. Run: pip install -e . (to install new entry point)
    5. Run: exp-run-optimize --help (should show usage)
    6. Run: python -c "from mlflow_tracking import main_optimize, exp_run_optimize_command; print('Import successful')"

    **Expected output:**
    - CLI help shows --config, --n-trials, --n-jobs, --verbose arguments
    - Import succeeds without errors
    - Entry point installed and callable from command line
  </verify>
  <done>
    CLI extension (exp-run-optimize) provides convenient interface for optimization
    Supports --n-trials override, --n-jobs for parallel trials, --verbose for progress
    Saves best config as YAML for easy reuse
    Returns proper exit codes (0=success, 1=optimization error, 2=config error)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create example optimization configs and test suite</name>
  <files>examples/configs/optimization/, mlflow_tracking/test_optuna_optimizer.py</files>
  <action>
    **A. Create example optimization configurations (3 files):**

    1. Create examples/configs/optimization/ directory

    2. Create examples/configs/optimization/01_effnet_lr_search.yaml (learning rate search)

    3. Create examples/configs/optimization/02_ridge_alpha_search.yaml (alpha search)

    4. Create examples/configs/optimization/03_xgboost_multi_param.yaml (multi-parameter search)

    5. Create examples/configs/optimization/README.md (150+ lines) with:
       - Introduction to hyperparameter optimization with Optuna
       - Search space syntax (float, int, categorical types)
       - Pruner types (median, hyperband, successive_halving)
       - CLI usage examples
       - Optimization config schema reference
       - Best practices for hyperparameter optimization
       - Interpreting optimization results

    **B. Create mlflow_tracking/test_optuna_optimizer.py (200+ lines):**

    1. Test OptimizationConfig schema validation
    2. Test OptunaOptimizer initialization
    3. Test optimization study (dry run, no actual training)
    4. Test CLI integration
    5. Add mock trials for testing without actual training

    **C. Update examples/configs/README.md (50+ lines):**
    - Add section "Hyperparameter Optimization"
    - Add example CLI commands
    - Add best config usage instructions

    **Key Implementation Details:**
    - Use mock trials in tests to avoid actual training
    - Test all parameter types (float, int, categorical)
    - Test all pruner types (median, hyperband, successive_halving)
    - Validate best config is valid ExperimentConfig
    - Document best practices: start with coarse search, refine with fine search
  </action>
  <verify>
    **Verification steps:**
    1. Check examples/configs/optimization/ directory exists with 3 example configs
    2. Check examples/configs/optimization/README.md exists (150+ lines)
    3. Check mlflow_tracking/test_optuna_optimizer.py exists (200+ lines)
    4. Run: python -m pytest mlflow_tracking/test_optuna_optimizer.py -v
    5. Run: python -c "from mlflow_tracking.config_parser import ConfigParser; config = ConfigParser.load_config('examples/configs/optimization/01_effnet_lr_search.yaml'); print(f'Config loaded: {config.optimization.study_name}')"
    6. Run: exp-run-optimize examples/configs/optimization/01_effnet_lr_search.yaml --help (should show usage)

    **Expected output:**
    - All tests pass
    - Config loads successfully with optimization section
    - CLI shows help with correct arguments
    - Example configs demonstrate all search space types and pruners
  </verify>
  <done>
    Example optimization configs demonstrate float, int, and categorical search spaces
    Test suite validates OptimizationConfig schema, OptunaOptimizer, and CLI integration
    Documentation explains search space syntax, pruner types, CLI usage, and best practices
    Users can run hyperparameter optimization via: exp-run-optimize config.yaml
    Best configs saved as YAML for easy reuse: exp-run config_best.yaml
  </done>
</task>

</tasks>

<verification>
1. **OPT-01: Optuna Integration**
   - Run: exp-run-optimize examples/configs/optimization/01_effnet_lr_search.yaml --n-trials 5 --verbose
   - Verify: Optuna study created with correct search space
   - Verify: Trials execute using adapters and log to MLflow
   - Verify: Best params and best value printed at end

2. **OPT-02: Pruning Support**
   - Run optimization with pruner config (median, hyperband)
   - Verify: Pruner created correctly from config
   - Verify: Underperforming trials pruned early
   - Verify: MLflowCallback logs intermediate metrics for pruning decisions

3. **OPT-03: Parallel Trial Execution**
   - Run: exp-run-optimize examples/configs/optimization/01_effnet_lr_search.yaml --n-jobs 2
   - Verify: Multiple trials run in parallel
   - Verify: n_jobs parameter passed to study.optimize()
   - Verify: ResourceManager prevents GPU conflicts for parallel trials
</verification>

<success_criteria>
Phase 7 complete when:
- OptunaOptimizer class creates and runs Optuna studies with MLflow logging
- OptimizationConfig schema defines search spaces (float, int, categorical)
- CLI extension (exp-run-optimize) provides convenient optimization interface
- Pruner support (median, hyperband, successive_halving) saves computation
- Parallel trial execution (n_jobs) speeds up optimization
- Best configs saved as YAML for easy reuse
- Example configs and documentation demonstrate optimization workflow
- Test suite validates all functionality (schema, optimizer, CLI)
- Users can run: exp-run-optimize config.yaml --n-trials 100 --n-jobs 4
- Requirements OPT-01, OPT-02, OPT-03 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/07-hyperparameter-optimization**---optuna-integration-with-pruning/07-01-SUMMARY.md`

SUMMARY.md should include:
- Frontmatter with phase, plan, subsystem, tags, dependency graph, tech stack, key files, key decisions
- Performance metrics (duration, tasks complete, files created/modified)
- Task commits (atomic commits per task)
- Decisions made (Optuna integration patterns, pruner selection, parallel trial strategy)
- Deviations from plan (if any)
- Verification results (OPT-01, OPT-02, OPT-03 satisfied)
- Next phase readiness (all v1 requirements complete, framework production-ready)
</output>
