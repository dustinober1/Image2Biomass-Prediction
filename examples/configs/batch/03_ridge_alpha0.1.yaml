# Batch experiment 3: Ridge regression with alpha=0.1
# This config is part of a batch execution demonstration

experiment_name: sklearn_batch_demo
run_name: ridge_alpha0.1
adapter: sklearn
description: "Ridge regression on tabular metadata - alpha=0.1"

parameters:
  model_type: "ridge"
  alpha: 0.1
  fit_intercept: true
  random_seed: 42

tags:
  model_type: "linear"
  purpose: "batch_demo"
  features: "tabular"
  batch_config: "alpha0.1"

random_seed: 42
